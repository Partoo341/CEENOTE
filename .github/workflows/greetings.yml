name: Greetings

on: [pull_request, issues]

jobs:
  greeting:Niaje nani
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Thanks for making this decision'
        pr-message: 'Let's make this money together'
